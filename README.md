# 💣 ЛЕГЕНДАРНЫЙ ДАЙВИНЧИК: БОТ-АСИСТЕНТ ЗНАКОМСТВ v37.0 "СТАБИЛЬНЫЙ ЗАПУСК"

> **ВНИМАНИЕ! СРОЧНОЕ ЗАЯВЛЕНИЕ!**
>
> Этот код — не мой. Я его вытащил с какого-то форума, и, что самое смешное, тот, кто мне его дал, сам не в курсе, чей он. Так что, если что-то взорвется, все вопросы к мирозданию, а не ко мне. Используйте на свой страх и риск, это чистое, дикое наследие интернета!

## 🔥 Что это за БОМБА?

Это не просто скрипт, это **AI-Ассистент Знакомств** для Telegram, который работает на чистом питоне и нейросетях. Он был создан, чтобы вести диалоги за вас, имитируя живого человека с невероятной точностью. Версия **v37.0** — это апогей стабильности и скрытности.

Забудьте о скучных ответах. Этот бот — ваш личный кибер-сваха, который не просто отвечает, а **ДИАЛОГИРУЕТ**, используя продвинутую логику задержек и имитацию печати, чтобы никто и никогда не догадался, что вы спите, пока он завоевывает сердца.

## 🚀 ФУНКЦИОНАЛ, КОТОРЫЙ РВЕТ ЧАТЫ

*   **🧠 Глубокая AI-Генерация Ответов:** Использует мощь `openai` для создания контекстуально идеальных и убедительных сообщений.
*   **⏳ Режим "Живого Человека":** Уникальная система задержек и имитации печати (`ChatAction.TYPING`) с тремя режимами (`fast`, `medium`, `long`), чтобы ответ не выглядел мгновенным и роботизированным.
*   **📜 Сохранение Истории:** Все диалоги сохраняются в `conversation_histories.json`, позволяя AI помнить, о чем вы говорили.
*   **🛡️ Система Белых Списков:** Управление доступом через `whitelist.json` для контроля, с кем бот может общаться.
*   **🪜 "Лесенка" Ответов:** Поддержка разделения длинных ответов на несколько сообщений с помощью разделителя `|||` для еще большей имитации живого общения.

## 🛠️ КАК ЗАПУСТИТЬ ЭТОГО МОНСТРА

Для запуска вам понадобится Python 3.10+ и немного магии.

### 1. Клонирование и Подготовка

```bash
git clone https://github.com/qrrrv/the-sociator.git
cd дайвинчик
```

### 2. Установка Зависимостей

Все, что нужно для работы, находится в `requirements.txt`.

```bash
pip install -r requirements.txt
```

### 3. Конфигурация Секретов

Создайте файл `.env` в корне проекта и заполните его своими данными. **БЕЗ ЭТОГО НИЧЕГО НЕ ЗАРАБОТАЕТ!**

```ini
# Telegram API
API_ID=ВАШ_API_ID
API_HASH=ВАШ_API_HASH
BOT_USERNAME=ИМЯ_ПОЛЬЗОВАТЕЛЯ_БОТА_ДЛЯ_МОНИТОРИНГА

# OpenAI API
OPENAI_API_KEY=ВАШ_OPENAI_API_KEY
OPENAI_MODEL=gpt-3.5-turbo # Или любая другая модель, которую вы хотите использовать

# Настройки бота
TYPING_SPEED_CPS=10 # Скорость печати в символах в секунду (для имитации)
```

### 4. Запуск

Запускайте, и пусть чаты горят!

```bash
python main.py
```

## ⚙️ НАСТРОЙКА ДЛЯ ПРОФИ

*   **`whitelist.json`**: Добавьте сюда ID пользователей, с которыми бот должен вести диалог.
*   **`conversation_histories.json`**: Здесь хранится вся история. Можете очистить его, если бот "забыл", кто он.
*   **`main.py`**: Внутри есть секция `delay_config` для тонкой настройки задержек. Не трогайте, если не знаете, что делаете, иначе "живой человек" превратится в "робота-скорострела".

---
*P.S. Если найдете настоящего автора, передайте ему привет. Он создал шедевр.*
